[project]
name = "backend-ai-flyrank"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
    "uvicorn>=0.53.0",
]

[tool.uv.workspace]
members = [
    "Backend track/a3-containerized-stack",
    "Backend track/a4-auth-api",
]
